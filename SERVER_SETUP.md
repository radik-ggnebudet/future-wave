# 🚀 Простое развертывание на сервере Ubuntu 24.04

Инструкция по установке и запуску бота на сервере через SSH.

---

## 📋 ШАГ 1: Подключение к серверу

```bash
ssh root@ваш_ip_адрес
# или
ssh username@ваш_ip_адрес
```

---

## 📦 ШАГ 2: Установка необходимых пакетов

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Python 3, pip, venv и git
sudo apt install python3 python3-pip python3-venv git -y

# Проверяем версию Python (должна быть 3.10+)
python3 --version
```

---

## 📥 ШАГ 3: Клонирование проекта

```bash
# Переходим в домашнюю директорию
cd ~

# Клонируем репозиторий
git clone https://github.com/radik-ggnebudet/future-wave.git

# Переходим в папку проекта
cd future-wave
```

---

## 🔧 ШАГ 4: Настройка виртуального окружения

```bash
# Создаем виртуальное окружение
python3 -m venv venv

# Активируем виртуальное окружение
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

---

## ⚙️ ШАГ 5: Настройка .env файла

```bash
# Создаем .env файл
nano .env
```

Вставьте следующее содержимое:
```
BOT_TOKEN=8479534381:AAGEQcgLdNpej_8SKTvo1a6Zh7c2pqbYDOw
```

Сохраните файл:
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter`
- Нажмите `Ctrl + X` (выход)

---

## ✅ ШАГ 6: Проверка работы бота

```bash
# Тестовый запуск (чтобы убедиться, что всё работает)
python3 bot.py
```

Если бот запустился и написал "🤖 Бот запущен! Ожидание сообщений...", значит всё ок!

**Остановите бота:** Нажмите `Ctrl + C`

---

## 🔄 ШАГ 7: Настройка автозапуска через systemd

Создаем systemd service для автоматического запуска бота:

```bash
sudo nano /etc/systemd/system/future-wave-bot.service
```

Вставьте следующее содержимое (**ВАЖНО: замените `username` на ваше имя пользователя**):

```ini
[Unit]
Description=Future Wave Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/future-wave
Environment="PATH=/root/future-wave/venv/bin"
ExecStart=/root/future-wave/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Если вы НЕ root**, замените `/root/` на `/home/ваш_username/`:
```ini
User=ваш_username
WorkingDirectory=/home/ваш_username/future-wave
Environment="PATH=/home/ваш_username/future-wave/venv/bin"
ExecStart=/home/ваш_username/future-wave/venv/bin/python bot.py
```

Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

---

## 🚀 ШАГ 8: Запуск бота как службы

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск бота при загрузке системы
sudo systemctl enable future-wave-bot

# Запускаем бота
sudo systemctl start future-wave-bot

# Проверяем статус (должно быть "active (running)")
sudo systemctl status future-wave-bot
```

---

## 📊 Полезные команды для управления ботом

### Проверить статус:
```bash
sudo systemctl status future-wave-bot
```

### Остановить бота:
```bash
sudo systemctl stop future-wave-bot
```

### Запустить бота:
```bash
sudo systemctl start future-wave-bot
```

### Перезапустить бота:
```bash
sudo systemctl restart future-wave-bot
```

### Просмотр логов (в реальном времени):
```bash
sudo journalctl -u future-wave-bot -f
```

### Просмотр последних 100 строк логов:
```bash
sudo journalctl -u future-wave-bot -n 100
```

### Отключить автозапуск:
```bash
sudo systemctl disable future-wave-bot
```

---

## 🔄 Обновление бота (когда будут изменения)

```bash
# Подключаемся к серверу
ssh root@ваш_ip_адрес

# Переходим в папку проекта
cd ~/future-wave

# Останавливаем бота
sudo systemctl stop future-wave-bot

# Получаем новые изменения
git pull

# Активируем виртуальное окружение
source venv/bin/activate

# Обновляем зависимости (на случай если добавились новые)
pip install -r requirements.txt

# Запускаем бота
sudo systemctl start future-wave-bot

# Проверяем что всё работает
sudo systemctl status future-wave-bot
```

---

## 🐛 Решение проблем

### Бот не запускается?

1. **Проверьте логи:**
```bash
sudo journalctl -u future-wave-bot -n 50
```

2. **Проверьте, что токен правильный в .env:**
```bash
cat ~/.env
```

3. **Проверьте права на файлы:**
```bash
ls -la ~/future-wave/
```

4. **Попробуйте запустить вручную:**
```bash
cd ~/future-wave
source venv/bin/activate
python3 bot.py
```

### Ошибка "Permission denied"?

```bash
# Дайте права на выполнение
chmod +x ~/future-wave/bot.py
```

### Бот работает, но не отвечает?

1. Проверьте, что токен правильный
2. Проверьте интернет-соединение на сервере:
```bash
ping telegram.org
```

---

## ✅ Готово!

Теперь ваш бот работает 24/7 на сервере и автоматически перезапустится:
- ✅ При падении
- ✅ При перезагрузке сервера
- ✅ При обновлении системы

**Бот доступен по имени в Telegram!** Напишите ему /start и проверьте работу.

---

## 📝 Краткая шпаргалка команд

```bash
# Статус
sudo systemctl status future-wave-bot

# Логи
sudo journalctl -u future-wave-bot -f

# Перезапуск
sudo systemctl restart future-wave-bot

# Обновление
cd ~/future-wave && git pull && sudo systemctl restart future-wave-bot
```

